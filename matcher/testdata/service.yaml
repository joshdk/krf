apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: custom-app
  labels:
    app: myapp
spec:
  selector:
    app.kubernetes.io/name: myapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
